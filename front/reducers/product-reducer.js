const _defaultState = {};

const productReducer = ((oldState = _defaultState, action) => {
  switch(action.type) {
    case 1 :

    break;

    case 2 :

    break;

    default:
    return oldState;
  }
}

export default productReducer;
